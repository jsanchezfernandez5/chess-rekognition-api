# ═══════════════════════════════════════════════════════════════
# requirements.txt — Chess Rekognition API
# Instalación de dependencias para la API REST de Chess Rekognition
# uso: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# FastAPI: framework principal de la API REST. Incluye soporte para async, validación automática y Swagger UI
fastapi==0.135.1

# Servidor ASGI recomendado para producción. Soporta HTTP/2, WebSocket y es muy eficiente
uvicorn[standard]==0.41.0

# SQLAlchemy: ORM para mapear las tablas MySQL a clases Python
sqlalchemy==2.0.36

# PyMySQL: driver de conexión Python a MySQL/MariaDB
pymysql==1.1.1

# Autenticación y seguridad (JWT: generación y verificación de tokens, hashing de contraseñas y backends de passlib)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Validación y configuración. El extra [email] añade soporte para EmailStr en los schemas
pydantic[email]==2.12.5

# pydantic-settings: carga y valida las variables del archivo .env
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Necesario para recibir Form() que extrae los datos del request y UploadFile en FastAPI
python-multipart==0.0.17

# Cliente oficial de Resend para envío de emails transaccionales
resend==2.10.0

# Backend de concurrencia requerido por FastAPI internamente
anyio==4.12.1

# Necesario para servir archivos estáticos (favicon, etc.)
aiofiles==24.1.0

# Procesamiento de imagen y utilidades científicas. 
# OpenCV se usa para la corrección de perspectiva y preprocesamiento de las imágenes de los tableros
# (la variante headless se instala al FINAL del fichero, ver último bloque)
numpy==2.2.3

# Reglas de ajedrez y validación de movimientos
python-chess==1.999

# Wrapper Python para el binario de Stockfish (gestiona el protocolo UCI)
stockfish>=3.28.0

# Motor de detección de objetos YOLO26 (motor principal y único de reconocimiento).
# La versión 8.4.x introduce la arquitectura YOLO26 (yolo26n.pt, yolo26s.pt...).
# AVISO DE PESO: arrastra PyTorch (~200 MB de descarga y ~2 GB instalado). Aumenta el build
# y los arranques en frío del despliegue en Railway. Si el build se queda sin espacio o el
# despliegue se degrada, valorar instalarlo solo donde se entrene/detecte o una imagen base
# con PyTorch preinstalado. La API arranca igualmente si este paquete falta: el motor YOLO
# queda simplemente "no listo" (import diferido en services/yolo_detector.py).
ultralytics>=8.4.120

# ⚠️ Variante headless de OpenCV (sin soporte GUI/X11). Debe quedar como ÚLTIMO paquete
# relacionado con OpenCV del fichero: ultralytics declara "opencv-python" (variante completa,
# que necesita libxcb.so.1/X11 y no existe en el contenedor de Railway) y ambos escriben en
# site-packages/cv2/, así que el último en instalarse gana. No usamos ninguna función GUI
# (ni cv2.imshow ni .show()), por lo que forzar headless no quita funcionalidad real.
# Defensa adicional garantista en nixpacks.toml (force-reinstall tras el pip install normal).
opencv-python-headless>=4.8.0
